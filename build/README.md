Place the production macOS app icon here as `icon.icns`.

Recommended source sizes before converting to `.icns`:
- 512x512
- 1024x1024

electron-builder will use Electron's default icon until `build/icon.icns` exists.
